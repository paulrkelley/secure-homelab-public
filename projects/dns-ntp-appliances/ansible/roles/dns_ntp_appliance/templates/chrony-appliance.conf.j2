# Sanitized public example.
# Use approved production sources in private configuration.

# Example placeholder only:
# pool {{ chrony_upstream_pool }} iburst

rtcsync
makestep 1 3
maxupdateskew 100.0
ratelimit

{% if chrony_enable_server %}
{% for network in chrony_allowed_networks %}
allow {{ network }}
{% endfor %}
{% else %}
# Server mode intentionally disabled in the public/default scaffold.
{% endif %}
